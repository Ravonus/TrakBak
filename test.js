var socket = io('{{server}}');
var trakbakSocket = '{{serverBack}}';
var trakbak = {};

socket.on('connected', function (data) {
  console.log('dawgs n shit on connect.');
});

socket.on('me', function (data) {
  if (!JSON.parse(data.msg).error) {

    trakbak.user = JSON.parse(data.msg);
    checkRegistration();
    sideNav();
    //  selectSocket(['domains', 'paths', 'postbacks']);
  } else {

    localStorage.removeItem("trakbak");
    window.location.href = "/";

  }
});


//Don't edit after this line. Edit inside of clients folder.//

console.log("TESSST")var socket = io('{{server}}');
var trakbakSocket = '{{serverBack}}';
var trakbak = {};

socket.on('connected', function (data) {
  console.log('dawgs n shit on connect.');
});

socket.on('me', function (data) {
  if (!JSON.parse(data.msg).error) {

    trakbak.user = JSON.parse(data.msg);
    checkRegistration();
    sideNav();
    //  selectSocket(['domains', 'paths', 'postbacks']);
  } else {

    localStorage.removeItem("trakbak");
    window.location.href = "/";

  }
});


//Don't edit after this line. Edit inside of clients folder.//

console.log("TESSST")var socket = io('{{server}}');
var trakbakSocket = '{{serverBack}}';
var trakbak = {};

socket.on('connected', function (data) {
  console.log('dawgs n shit on connect.');
});

socket.on('me', function (data) {
  if (!JSON.parse(data.msg).error) {

    trakbak.user = JSON.parse(data.msg);
    checkRegistration();
    sideNav();
    //  selectSocket(['domains', 'paths', 'postbacks']);
  } else {

    localStorage.removeItem("trakbak");
    window.location.href = "/";

  }
});


//Don't edit after this line. Edit inside of clients folder.//

console.log("TESSST")var socket = io('{{server}}');
var trakbakSocket = '{{serverBack}}';
var trakbak = {};

socket.on('connected', function (data) {
  console.log('dawgs n shit on connect.');
});

socket.on('me', function (data) {
  if (!JSON.parse(data.msg).error) {

    trakbak.user = JSON.parse(data.msg);
    checkRegistration();
    sideNav();
    //  selectSocket(['domains', 'paths', 'postbacks']);
  } else {

    localStorage.removeItem("trakbak");
    window.location.href = "/";

  }
});


//Don't edit after this line. Edit inside of clients folder.//

console.log("TESSST")var socket = io('{{server}}');
var trakbakSocket = '{{serverBack}}';
var trakbak = {};

socket.on('connected', function (data) {
  console.log('dawgs n shit on connect.');
});

socket.on('me', function (data) {
  if (!JSON.parse(data.msg).error) {

    trakbak.user = JSON.parse(data.msg);
    checkRegistration();
    sideNav();
    //  selectSocket(['domains', 'paths', 'postbacks']);
  } else {

    localStorage.removeItem("trakbak");
    window.location.href = "/";

  }
});


//Don't edit after this line. Edit inside of clients folder.//

console.log("TESSST")var socket = io('{{server}}');
var trakbakSocket = '{{serverBack}}';
var trakbak = {};

socket.on('connected', function (data) {
  console.log('dawgs n shit on connect.');
});

socket.on('me', function (data) {
  if (!JSON.parse(data.msg).error) {

    trakbak.user = JSON.parse(data.msg);
    checkRegistration();
    sideNav();
    //  selectSocket(['domains', 'paths', 'postbacks']);
  } else {

    localStorage.removeItem("trakbak");
    window.location.href = "/";

  }
});


//Don't edit after this line. Edit inside of clients folder.//

console.log("TESSST")var socket = io('{{server}}');
var trakbakSocket = '{{serverBack}}';
var trakbak = {};

socket.on('connected', function (data) {
  console.log('dawgs n shit on connect.');
});

socket.on('me', function (data) {
  if (!JSON.parse(data.msg).error) {

    trakbak.user = JSON.parse(data.msg);
    checkRegistration();
    sideNav();
    //  selectSocket(['domains', 'paths', 'postbacks']);
  } else {

    localStorage.removeItem("trakbak");
    window.location.href = "/";

  }
});


//Don't edit after this line. Edit inside of clients folder.//

console.log("TESSST")